namespace VoiceConcierge.Data.Enums;

/// <summary>
/// Four voices required by PRD (VX-1). Stored as string in DB.
/// </summary>
public enum VoiceId
{
    James = 0,
    Sofia = 1,
    Marcus = 2,
    Elena = 3,
}
